:hierarchy:
    - "nodes/%{::hostname}"
    - "modules/%{calling_module}"
    - "role/%{::role}"
    - "domain/%{::domain}/%{::role}"
    - common
:backends:
    - yaml
:merge_behaviour: deeper
:yaml:
    :datadir: '/hieradata'
